---
date: 2026-06-13
---
## Decisions Made
- Adopted Agent OS memory system — "continue where we left off" found nothing because no session was ever captured; scaffolded memory/ + wiki/ + AGENTS.md (gitignored).
- Split scan-derived work into Tier A (genuine fixes) vs Tier B (initiatives). "Fix all" → did all real fixes; initiatives flagged for decisions rather than guessed.
- Attribution governs by *authorship* not initiator: machine author behind a human-initiated release takes over governance (tightening-only; initiator preserved in audit). Reused the built-but-unused attribution.Detector.
- Reputation threshold + calibration accuracy made config-driven (zero = built-in default) instead of hardcoded.
- Earned trust = the one path that LOOSENS (reputation raises trust → auto-approve). Safe via: opt-in, non-spoofable stored outcomes, sample-gated, raise-only, necessary-not-sufficient.
- Darwin sandbox via sandbox-exec (not Docker — single-binary product); deny external egress but keep loopback so go-plugin gRPC handshake survives. Real enforcement test included.
- Identity grants reuse built-but-unused identity.FileStore+Registry; trustScore→level, kind+local-name match, raise-only, composes with earned trust (higher wins).
- MCP split is pure code-movement, same package, tests pin parity (2417→680 server.go).
- Stacked PRs where work depends on prior (governance), independent branches off main otherwise (cli/mcp/darwin).

## What Went Wrong
- MCP split: off-by-one extracting the import block (missed closing `)` at line 26) — caught by build, regenerated from git HEAD.
- goimports cannot auto-add the aliased `cgpsdk` import — had to add it manually after moving the CGP handlers.
- Forgot to recreate the refactor/mcp branch after an earlier cleanup; committed the MCP split onto local main. Fixed: `git branch <name>` at HEAD, then `git reset --hard origin/main`.
- Wrote a duplicate identity FileStore before noticing file_store.go already existed — deleted the dup, used the existing one.
- memory/ diverges per branch (each carries its own snapshot) — capture landed on feat/identity-trust.

## Open Questions
- Merge sequencing: stack #158→#159→#163 must merge in order; rebase needed if any base changes.
- Should memory/ really be tracked in the shared repo (it's per-dev agent state)? Flagged at scaffold time; user kept it tracked.
- Capability-based identity grants (registry Capabilities action/scope) — deferred follow-up.

## Tasks
Done: Agent OS scaffold; Tier A (attribution wiring, reputation/calibration config, post-calibration validation, E2E test); Tier B (earned trust, CLI exit-code coverage, MCP decomposition, darwin sandbox-exec, identity grants). 6 PRs opened (#158-#163).
Remaining: review/merge the 6 PRs in order; capability-based identity grants (roadmap Later); pre-existing macOS git-test flake (not ours).
