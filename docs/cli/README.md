# CLI Reference

> **Generated file — do not edit by hand.**
> These pages are produced from the cobra command tree. Regenerate with:
> ```
> make docs-cli
> ```
> Edit flag/command help text in `internal/cli/*.go`, then regenerate and commit.

Start at [`relicta.md`](relicta.md) for the top-level command, then follow the
links into each subcommand. Every page lists the command's synopsis, flags, and
the global flags inherited from `relicta`.

For task-oriented guides see [quick-start](../quick-start.md) (zero-config
onboarding) and the [setup wizard guide](../wizard-guide.md) (`relicta init
--guided`).
